# to take the input from the user we invoke input() method to take input from user
# the input()  method takes the input in the form of string only (by default) is on the line 9
name = input("Enter your name : ")

print (name)

number = input("Enter a number : ")

print(name + number)