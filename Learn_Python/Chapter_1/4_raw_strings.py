# invoking the r before the string start, treats all the esacpe sequences as normal text
# Note : please do not give space after the r because it throws error
print(r"This is the new lines escape sequence : \n hello")

print(r"this is the tab escape sequence : \t hello")